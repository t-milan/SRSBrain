<UserControl x:Class="Plugin.TargetListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plugin"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="540">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <GroupBox Grid.Column="0" Header="Select Target Structrues" Margin="5">
                    <ListView ItemsSource="{Binding SelectableTargets}">
                        <ListView.View>
                            <GridView>
                                <GridView.Columns>
                                    <GridViewColumn Header="">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding isChecked}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding structure.Id}" Header="Id"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding structure.DicomType}" Header="Volume Type"/>
                                    <GridViewColumn Header="Volume">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding structure.Volume, StringFormat=N2}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView.Columns>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </GroupBox>
                <GroupBox Grid.Column="1" Header="Field Arrangement" Margin="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ComboBox Grid.Row="0" ItemsSource="{Binding Arrangements}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedArrangement}"/>
                        <Grid Margin="5" Grid.Row="1">
                            <Image Source="{Binding SelectedArrangement.ImagePath}" Stretch="Uniform" Width="200" MinHeight="220"/>
                        </Grid>
                        <CheckBox Content="Automate Isocentre Placement" IsChecked="{Binding AutomateIso, Mode=TwoWay}" Grid.Row="2" Margin="5"/>
                        <CheckBox Content="Optimise Collimator Rotation" IsChecked="{Binding OptimiseCol, Mode=TwoWay}" Grid.Row="3" Margin="5"/>
                    </Grid>
                </GroupBox >
                
            </Grid>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="0.2*"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Margin="5" Value="{Binding Progress}"/>
            <Button Grid.Column="1" Content="Calculate" Margin="5" Command="{Binding CalculateOptimalCollimatorCommand}" />
        </Grid>
    </Grid>
</UserControl>
